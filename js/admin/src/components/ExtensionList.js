import Component from 'flarum/Component';
import ExtensionListItem from 'flagrow/bazaar/components/ExtensionListItem';
import Button from 'flarum/components/Button';
import BazaarLoader from 'flagrow/bazaar/components/BazaarLoader';
import ExtensionPlaceholder from 'flagrow/bazaar/components/ExtensionPlaceholder';

/**
 * The `DiscussionList` component displays a list of discussions.
 *
 * ### Props
 *
 * - `params` A map of parameters used to construct a refined parameter object
 *   to send along in the API request to get discussion results.
 */
export default class ExtensionList extends Component {

}
